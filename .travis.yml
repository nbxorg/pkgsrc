dist: trusty
sudo: required
language: erlang 
install: skip

before_script:
  - git clone --depth 1 https://github.com/nbxorg/pkgbuild.git

script:
  - wget -O- https://tmvg.home.xs4all.nl/travis/debootstrap.sh | sh -

branches:
  except:
    - /^debian\/\d/

git:
  depth: 3
  lfs_skip_smudge: true

addons:
  apt:
    packages:
    - ca-certificates
    - debootstrap
